# Google AI Studio key - https://aistudio.google.com/apikey
# Used for embeddings (stage 2) and adjudication/repair (stages 3-4).
# Stage 1 needs no key: it makes no network calls, which is the point.
GEMINI_API_KEY=
